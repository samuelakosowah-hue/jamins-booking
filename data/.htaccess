# The database, sessions and error log live here. Never serve any of it over the web.
# (Apache 2.4 syntax first, then 2.2 as a fallback.)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
